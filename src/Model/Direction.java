package Model;

/*
* --This enum is used to represent the direction of the mower.
* --This enum has four values --EAST, WEST, SOUTHEAST, SOUTHWEST
*/
public enum Direction {
    EAST,
    WEST,
    SOUTHEAST,
    SOUTHWEST
}
